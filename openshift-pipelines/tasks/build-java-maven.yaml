apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: build-java-maven
spec:
  description: >-
    Builds a Java Maven project using the Maven wrapper.
  params:
    - name: context-dir
      type: string
      default: "."
      description: Subdirectory within the workspace containing the project
    - name: MAVEN_GOALS
      type: string
      default: "package"
      description: Maven goals to execute
    - name: SKIP_TESTS
      type: string
      default: "true"
      description: Whether to skip tests during the build
  workspaces:
    - name: source
      description: The workspace containing the cloned source code
  steps:
    - name: build
      image: registry.redhat.io/ubi9/openjdk-21:latest
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      workingDir: $(workspaces.source.path)/$(params.context-dir)
      script: |
        #!/usr/bin/env bash
        set -euo pipefail

        if [ -f "mvnw" ]; then
          chmod +x mvnw
          ./mvnw $(params.MAVEN_GOALS) \
            -DskipTests=$(params.SKIP_TESTS) \
            -Dquarkus.package.jar.type=fast-jar \
            --no-transfer-progress
        else
          echo "ERROR: Maven wrapper not found"
          exit 1
        fi
      env:
        - name: MAVEN_OPTS
          value: "-Xmx1024m"
